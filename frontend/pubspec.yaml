name: vehicle_service_manager_frontend
description: Flutter Web frontend for a Vehicle Service & Maintenance Manager.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  go_router: ^13.0.0
  shared_preferences: ^2.2.0
  intl: ^0.19.0
  uuid: ^4.3.0
  idb_shim: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

